{
  "mcpServers": {
    "code-mode": {
      "command": "npx",
      "args": [
        "@anthropic-ai/srt",
        "run",
        "node",
        "/Users/chenwenjie/Downloads/mcp-code-mode-demo/dist/server.js"
      ],
      "description": "Code Mode - TypeScript 沙箱执行，内置 filesystem 和 fetch 工具（通过 srt 代理实现网络访问）。",
      "env": {
        "NODE_OPTIONS": "--no-warnings"
      }
    }
  }
}
