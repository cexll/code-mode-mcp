/**
 * Deprecated: Code Mode Agent 示例已移除
 *
 * 项目已统一为 MCP Server + SimpleSandbox 架构。
 * 如需运行，请使用：
 *   - `npm run server`            启动 MCP 服务器
 *   - 参考 README.md 中的说明
 */
function main() {
  console.log(
    [
      '⚠️  examples/chat.ts 已弃用',
      '项目已切换到 MCP Server + SimpleSandbox 架构。',
      '请使用 `npm run server` 或参考 README.md。',
    ].join('\n')
  );
}

main();
